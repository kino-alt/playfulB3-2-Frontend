import {DiscussionTime} from "@/src/components/discussion-time"

export default function DiscussionTimePage({
  params,
}: {
  params: { id: string }
}) {
  // const { id } = params;
  return (
    <DiscussionTime />
  )
}
