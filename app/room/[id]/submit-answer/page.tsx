import SubmitAnswer from "@/src/components/submit-answer"

export default function SubmitAnswerPage({
  params,
}: {
  params: { id: string }
}) {
  // const { id } = params;
  return (
    <SubmitAnswer />
  )
}
